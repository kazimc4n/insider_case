Application deployed successfully!

Get the service URL:
  export NODE_PORT=$(kubectl get svc {{ include "insider-case.fullname" . }} -o jsonpath="{.spec.ports[0].nodePort}")
  export NODE_IP=$(minikube ip)
  echo "http://$NODE_IP:$NODE_PORT/ping"
