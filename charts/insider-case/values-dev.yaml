replicaCount: 1

image:
  tag: dev

# Local minikube uses `minikube image load`; no GHCR pull secret required.
imagePullSecrets: []

resources:
  requests:
    cpu: 50m
    memory: 32Mi
  limits:
    cpu: 100m
    memory: 64Mi