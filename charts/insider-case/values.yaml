replicaCount: 1

image:
  repository: insider-case
  tag: dev
  pullPolicy: IfNotPresent

service:
  type: NodePort
  port: 80
  targetPort: 8080

resources:
  requests:
    cpu: 50m
    memory: 32Mi
  limits:
    cpu: 100m
    memory: 64Mi

env:
  PORT: "8080"
  GIT_SHA: "dev"